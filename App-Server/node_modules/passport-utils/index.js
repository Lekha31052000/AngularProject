/**
 * Module dependencies.
 */
var OAuth2Strategy = require('./lib/strategies/oauth2'),
    InternalOAuthError = require('./lib/errors/internaloautherror'),
    Strategy = require('./lib/strategies/strategy');


/**
 * Framework version.
 */
require('pkginfo')(module, 'version');

/**
 * Expose strategy constructors.
 */
exports.OAuth2Strategy = OAuth2Strategy;
exports.Strategy = Strategy;

/**
 * Expose error constructors
 */
exports.InternalOAuthError = InternalOAuthError;
